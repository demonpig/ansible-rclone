---

# Type: string
#
# Description:
# Version of the rclone to install on the managed host. The role will search
# the application's tags in its repository. The role will fail if it can't
# find the tag.
rclone_version: latest
